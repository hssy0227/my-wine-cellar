# CLAUDE.md — 프로젝트 작업 지침

이 파일은 Claude Code가 이 저장소에서 작업할 때 **가장 먼저 읽는 문서**다.
새 세션이 시작될 때마다 여기서 맥락을 잡는다.

---

## 이 프로젝트가 하는 일

한국 와인 애호가가 **한글로 입력한 와인 이름을 원어명으로 매칭**하는 검색 엔진과 사전.

Vivino·CellarTracker 등 해외 서비스는 원어(라틴 문자) 기준으로만 검색된다.
"샤또 딸보"로는 아무것도 안 나오고 "Chateau Talbot"으로 쳐야 한다.
한글 음차 표기는 사람마다 달라서(딸보/탈보/타르보) 단순 문자열 매칭으로는 잡히지 않는다.

이 프로젝트는 그 간극을 메운다.

---

## 절대 어기면 안 되는 규칙

### 1. Python과 JS의 정규화 테이블은 반드시 동일해야 한다

자모 정규화 규칙이 **두 곳에 중복 정의**되어 있다:

- `src/matcher/jamo_matcher.py` — 사전 빌드 시 검색 키(`key_ko`)를 만든다
- `src/matcher/matcher.js` — 런타임에 사용자 질의를 같은 방식으로 변환한다

두 파일의 `CONSONANT_FOLD` / `VOWEL_FOLD` / `CHEAP` 값이 **한 글자라도 다르면
저장된 키와 조회 키가 어긋나 매칭이 조용히 깨진다.**

> 실제 사고 사례: JS에만 반모음 규칙(ㅑ→ㅏ)을 추가하고 Python에는 안 넣어서,
> "가야"(Gaja)가 엉뚱한 "Cowra"로 매칭됐다. 에러도 안 나고 결과만 틀렸다.

**정규화 규칙을 건드리면 반드시 두 파일을 함께 수정하고, `npm test`로 확인한다.**
테스트에 이 동기화를 검증하는 케이스가 있다(`tests/test_sync.py`).

### 2. 아포스트로피·하이픈은 공백이 아니라 삭제한다

`d'Abruzzo`, `Pontet-Canet`처럼 이 기호들은 단어를 **가르는 게 아니라 이어붙인다**.
공백으로 치환하면 `d abruzzo`가 되어 토큰 경계가 어긋나고 매칭이 실패한다.

`search_key_en()`(Python)과 `normEn()`(JS) 양쪽에 같은 규칙이 있다. 위 규칙 1과 동일하게 취급.

### 3. 사전의 원천은 Supabase다. git의 CSV는 export 사본이다

**`data/seed/`와 `data/dist/`를 직접 편집하지 않는다.** 둘 다 Supabase에서
자동 생성되어 커밋되며, 손으로 고쳐도 다음 동기화에 덮어써진다.

데이터를 고치는 경로는 하나다 — 배포 페이지의 톱니바퀴(관리 패널).
편집하면 즉시 사전에 반영되고, GitHub Action이 잠시 뒤 git에 동기화한다.

> git은 폐기된 게 아니라 **강등**됐다. 여전히 필수다:
> 회귀 테스트의 골든 데이터이고(아래 규칙 4), Supabase가 정지·삭제돼도
> 사전을 복구할 수 있는 완전한 사본이며, 배포 페이지의 최후 폴백이다.
> 그래서 동기화 실패는 조용히 넘어가지 않고 이슈를 연다.

상세는 `docs/SUPABASE.md`.

### 4. 파생 로직은 `build.py` 한 곳에만 존재한다

canonical_id 부여, 대표 표기 선정, `key_ko`/`key_en` 계산은 **`scripts/build.py`가
유일한 구현이다.** SQL 트리거·JS·Edge Function 어디에도 재구현하지 않는다.

검증은 중복해도 된다 — DB `CHECK` 제약이 Python 검증과 겹치는 건 환영한다.
실패 양상이 다르기 때문이다:

| 무엇이 어긋나면 | 어떻게 드러나는가 |
|---|---|
| 중복된 **검증** | 제약 위반으로 요청이 거부된다. **시끄럽다** |
| 중복된 **파생** | 검색 결과만 조용히 틀린다. **에러가 안 난다** |

두 번째가 규칙 1의 가야→Cowra 사고다. `search_key_en`을 Postgres의
`unaccent`+`regexp_replace`로 옮기고 싶어질 때가 있는데, `d'Abruzzo`나
`Pontet-Canet`에서 Python과 한 글자만 갈려도 유니크 인덱스가 조용히 오염된다.
그래서 `terms.key_en_norm`은 **Python이 계산해서 넣고**, DB는 모양만 검사한다.

---

## 빌드

```bash
python scripts/build.py        # seed -> data/dist/ 전체 재생성
npm test                       # 회귀 테스트 (반드시 통과해야 함)
python scripts/audit.py        # 데이터 무결성 점검
```

빌드는 **멱등(idempotent)** 하다. 몇 번을 돌려도 같은 결과가 나온다.
(이전 버전은 스크립트를 순서대로 실행해야만 올바른 결과가 나오는 구조였다. 이관하며 개선했다.)

---

## 데이터 모델 핵심 개념

### canonical_id — 같은 대상을 묶는 그룹

"샤토 마고", "샤또 마고", "Chateau Margaux"는 전부 같은 대상이다.
이들은 같은 `canonical_id`를 공유하고, 검색 결과에는 대표 표기(`name_ko_primary`) 하나만 보여준다.

`is_alias=1`인 행은 별칭이다. 검색은 되지만 결과 표시에는 대표 표기가 쓰인다.

### type — 3종

| 값 | 뜻 |
|---|---|
| `grape` | 품종 (카베르네 소비뇽) |
| `region` | 지역·아펠라시옹 (포이약, 바롤로) |
| `producer` | 생산자·퀴베 (샤토 라투르, 오퍼스 원) |

**같은 이름이 두 타입에 속하는 건 정상이다.**
- `Montepulciano` = 품종이면서 지역 (품종+지역 합성 DOCG명)
- `Margaux` = 지역이면서 샤토명
- `Norton` = 미국 교배종 품종이면서 아르헨티나 와이너리

단, `producer`가 섞인 타입 중복은 대개 오류다. `audit.py`가 이를 경고로 잡는다.

---

## 자주 하는 실수와 판단 기준

### 별칭을 추가할까, 정규화 규칙을 고칠까?

**정규화가 이미 흡수하는 차이면 별칭이 불필요하다.**

| 차이 유형 | 예시 | 처리 |
|---|---|---|
| 된소리·거센소리 | 딸보 / 탈보 | 자동 흡수 — 별칭 불필요 |
| 띄어쓰기 | 샤또마고 / 샤또 마고 | 자동 흡수 — 별칭 불필요 |
| 반모음 | 쪼 / 쬬 | 자동 흡수 — 별칭 불필요 |
| **모음이 실제로 다름** | 디상 / 디샹 | **별칭 필요** |
| **음절 구성이 다름** | 샤르도네 / 샤도네이 | **별칭 필요** |

판단이 애매하면 `data/dist/wine_terms.csv`에 넣어보고 테스트 도구에서 확인한다.
무작정 정규화 규칙에 넣으면 서로 다른 이름끼리 오매칭이 늘어난다.

### 새 항목이 정말 신규인가, 동의어인가?

같은 품종이 언어권별로 다른 이름을 갖는 경우가 많다.

```
Grenache = Garnacha          Mourvèdre = Monastrell
Syrah = Shiraz               Zinfandel = Primitivo
Pinot Gris = Pinot Grigio    Macabeo = Viura
```

이걸 각각 독립 항목으로 만들면 **같은 품종이 두 개의 캐노니컬로 쪼개진다.**
새 품종을 추가하기 전에 `audit.py`의 "동일 한글명 → 다른 원어" 항목을 확인한다.

### 원어명 일부를 빠뜨리지 않았는가

과거에 실제로 발생한 오류:

```
"모스카토 다스티" -> "Moscato"      (d'Asti 유실)
"소노마 코스트"   -> "Sonoma"        (Coast 유실)
"토카이 아수"     -> "Tokaj"         (Aszú 유실)
```

한글명은 여러 어절인데 원어명이 한 단어면 의심해야 한다. `audit.py`가 이 패턴을 잡는다.

---

## 코드 스타일

- 주석은 **왜**를 설명한다. 무엇을 하는지는 코드가 말한다.
- 특히 매칭 알고리즘의 상수(0.3, 0.6, 0.80 등)에는 반드시 근거를 적는다.
  나중에 누군가 "이 숫자 왜 이래?" 하고 바꾸면 회귀가 난다.
- 한국어 주석을 쓴다. 이 프로젝트의 도메인 지식이 한국어 맥락에 있다.

---

## 관련 문서

| 문서 | 내용 |
|---|---|
| `docs/REQUIREMENTS.md` | 앱이 무엇을 해야 하는가 |
| `docs/ARCHITECTURE.md` | 매칭 엔진이 어떻게 동작하는가 |
| `docs/DATA_MODEL.md` | 데이터 스키마 상세 |
| `docs/MIGRATION.md` | 이전 상태에서 이 저장소로 옮긴 경위 |
| `docs/DECISIONS.md` | 설계 판단과 그 근거 (왜 그렇게 했는가) |
